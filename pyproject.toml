[project]
name = "atlasul-aerului"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pyarrow>=25.0.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.7",
]

[tool.pytest.ini_options]
pythonpath = ["."]
